import styles from './GetAllCatalog.module.scss';

const GetAllCatalog = () => {
  return (
    <div className={styles.btn}>
      <span className={styles.text_btn}>СМОТРЕТЬ ВЕСЬ КАТАЛОГ</span>
    </div>
  );
};

export default GetAllCatalog;
