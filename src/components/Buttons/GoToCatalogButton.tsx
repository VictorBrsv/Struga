import styles from './GoToCatalogButton.module.scss';

const GoToCatalogButton = () => {
  return (
    <div className={styles.btn}>
      <span>ПЕРЕЙТИ В КАТАЛОГ</span>
    </div>
  );
};

export default GoToCatalogButton;
